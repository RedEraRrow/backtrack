# Backtrack — TODO

**Status:** ✅ done · 🟡 partial · ⬜ open
IDs (`#N`) are stable so cross-references keep working; items are grouped by area, not by number.

---

## Session handoff — read me first

**Architecture you'll be working with (all in `src/utils/prompt.py` unless noted):**
- **Unified list widget.** `select(message, choices, *, columns=None, multiple=False, interlock_category_callback=None, index=0, shortcuts=None, header=None, extra_hints=None)` is the ONE list widget. `checkbox(...)` is now just a wrapper calling `select(multiple=True)`. Single-select returns the chosen value (or None); multi-select returns a list of checked values (or None on cancel).
- **Structured columns (no string parsing anywhere).** Pass `columns=[Column(...), ...]` and give each `Choice(cells=[...])`. A `Column` has `style` (`'primary'`/`'static-dim'`/`'dynamic-dim'`/`'accent'`/`'normal'`), `align`, `flex` (absorbs leftover, truncates), `pin` (laid against the right edge), `min_width`/`max_width`/`max_frac`, `gap`. A **cell** is a `str`, a `(text, style)` tuple, or a **list of segments** (so one column mixes styles — e.g. column 1 = `[(TAG,'primary'), (' (friendly)','static-dim')]`). Helpers: `_table_widths`, `_render_table_row`, `_render_cell`, `_cell_segments`. The old `_split_columns`/title-parsers were deleted.
- **Never-wrap guarantee:** `_clip_ansi(line, width)` clips every emitted line (ANSI-aware) so lists never wrap. `_COLUMNS_MAX_WIDTH` (140) caps layout width on ultrawide terminals; `_EDGE_MARGIN` (4) keeps pinned columns off the edge.
- **Quit/back:** `q`/`Q` raise `QuitToTerminal` (caught in `main()`); value editors set `_state.QUIT_REQUESTED` to commit-then-quit. Back = `←`/`b`/`h`/`Esc`.
- **Focus-aware cursor:** focus reporting (`\033[?1004h`) is enabled in `ui_utils.enter_alt_screen`; `_read_key` emits `FOCUS_IN`/`FOCUS_OUT` and sets `_FOCUSED`; `_block_caret()`/`_char_caret()` draw hollow when blurred.
- **Tag display:** `id3_tag_handler.display_tag_id()` strips a trailing `:` from empty-descriptor keys (`APIC:` → `APIC`). `create_frame` dispatches album art on `ui_category == 'image'` (NOT `frame_type`); image payload is a dict `{'__image__', 'data', 'type', 'desc'}`. EQU2/RVA2 dispatch by base id.
- **Playback render:** `playback_ui._draw_default_ui` builds a frame buffer; `_render_frame_buffer` now only advances the row counter for FLOW lines (absolute-positioned items like the volume bar pass through without consuming a row — this was the metadata-displacement bug). `_meta_left_lines` does the title/artist/album + toggled extras.
- **Lyrics:** `save_sylt_entries` lives in `src/lyrics/lyrics.py` now (`lyric_timer.py` deleted).

**Verify before coding:** `pyright src` (currently 0/0) and a quick `python3 -c "import src.main, src.menus, src.playback.playback, ..."`. Python does NOT hot-reload — the app must be relaunched to see code changes (this bit us on #20/#82 below).

**All live TUI passes are DONE** (confirmed on a real terminal — see the archived section at the bottom). No verification backlog remains.

**Next session — ordered start-here list** (top = easiest/most self-contained; each is independent):

1. **#69 — More lyric imports** (needs design thought first). SYLT from json+md / json+uslt; USLT from txt/rtf/md. Pure parsing — add to `src/lyrics/`, unit-test the parsers. Pairs with #67.
2. **#67 — Lyrics-format doc** (with #69). A short md documenting the lyric file formats so the doc matches the importers.

(Scrapped: *regex in the Rename-tags op* — renaming only changes the 4-letter frame key, so a value find/replace there just duplicates *Set value*'s regex. No point.)

**Design-first (bring a decision before coding):** #23 (vim `:command` mode), #18 (general-userbase default settings), #64 (theming). *(#14 background/multi-window playback is now code-complete — see the Playback section — pending live multi-terminal validation.)*

**Blocked on environment (skip unless available):** #30 (needs a repro — may already be fixed), #73 (needs a mobile terminal).

---

## Navigation & input

- ✅ **#1 — Additional nav keys.** `←`/`b`/`h`/`Esc` = back, `→`/`l` = confirm, `↑↓`/`jk` = move, `q` = quit.
- ✅ **#10 — `q` = global save-and-quit to terminal.** Menus quit immediately; value editors commit the in-progress edit first, then quit on the next menu. `q` stays literal only in free-text fields.
- ✅ **#11 — Universal back button.** `←`/`b`/`h`/`Esc` returns from any `select()`/`checkbox` menu, including editors.
- ✅ **#22 — Logical, consistent hints.** Unified back/quit wording across widgets; `space` not `spc`; the right column is one `panel` hint.
- ✅ **#24 — Mouse clicks only register over text/buttons,** not the empty space in a row (`select()` + `checkbox()`).
- ✅ **#78 — Shared `select()` capabilities** underpinning the above: `separator()`/disabled rows (#7), `index=` to restore cursor (#5), `shortcuts=` (#27), `columns=` mode (#75), and **toggle-all (`a`)** in multi-select (checks all, or clears all when everything's ticked; auto-offered/hinted except where a category interlock or a caller `shortcuts['a']` would clash).
- 🟡 **#23 — Vim-like shortcut system.** `hjkl` navigation + `select(shortcuts=)` done. **Next idea (needs design):** a universal command mode — a `:` prompt that accepts short `:xyz` command codes (à la vim's command line) from anywhere in the app (e.g. `:play`, `:edit`, `:sort`, `:goto`, `:q`), so power users aren't limited to per-screen single-key shortcuts. Wants a thought-through command vocabulary + a shared command dispatcher before building.
- ✅ **#49 — Don't show hints for unavailable actions.** Conditional throughout: the `w`/panel hint only shows with content; multi-select's `space toggle` only in multiple mode; the value editors only list available keys.
- ✅ **#41 — `b` in playback returns to the previous page.** New `STOP` status: `b` stops playback and drops back to the browse list / action menu you came from (distinct from `n` next-track and `q` quit-app); shown in the help hints.
- ✅ **#52 — Esc backs out of text fields.** `Esc` cancels (→ None) in `text()` and `path()` (where `←`/`b` are literal/cursor); `Enter` still confirms.
- ✅ **#55 — Restore cursor on back.** `select(index=)` threaded through main menu, browse-by, and the group/album/track levels via `_idx_of`, so backing out lands on the item you came from. (Search/history results still one-shot.)
- ✅ **#58 — Two-step mouse select.** First click moves the cursor, second click confirms. Column rows act on any click; plain rows only act over the text itself.
- ✅ **Letter-index back navigation.** In the Artists/Albums A–Z index, backing out of a letter's filtered list now returns to the letter index (cursor restored onto that letter) instead of exiting the whole category. Only a drilled-in letter list does this; a plain or `/`-toggled full list still exits (`menus.py` LEVEL 2 back handler).
- ✅ **Skip single-option disc/work menu.** With the metadata editor hidden, selecting a disc- or work-header (whose only remaining action is "Play all") now plays immediately instead of popping a one-item menu — mirrors the track-level single-option auto-skip.
- ✅ **#85 — Arrow keys respond to a single tap in playback.** `get_key_non_blocking` (`terminal_input.py`) was reading one byte per loop iteration, so a 3-byte arrow escape took multiple iterations and the stale-escape flush often dropped a single tap (you had to hold the key). Now it assembles the whole escape in one call, and reads via `os.read(fd, …)` instead of the buffered `sys.stdin.read` so `select()` and the reads see the same OS buffer (the buffered reader had hidden the continuation bytes from `select`). Single arrow taps register in ~0.1 ms, matching the letter seek keys.

## Browse & library

- ✅ **#9 — Always show an album list** in artist/genre browse, even for a single album (no more single-album auto-skip).
- ✅ **#12 — Hide "Play all"/"Edit tags"** when a group/album has only one track (track + album level).
- ✅ **#25 — Richer track rows.** Featured-artist marker (when track artist ≠ album artist) + duration; duration is now cached.
- ✅ **#26 — Prefer tag data over filenames.** The metadata editor shows an explicit, clearly-labelled "File path" row marked as not ID3.
- ✅ **#28 — Absolute library paths.** `build_library` + config resolve via `abspath`/`expanduser`.
- ✅ **#33 — Prettier browse metadata.** Built on structured columns (#53). Track rows: bright title (truncates), the **full** featured artist in a dimmed left-aligned column, and a dimmed duration pinned further right with an edge margin. Album rows: album name + dimmed album artist. The album artist also shows in the header subtitle.
- ✅ **#34 — Cleaner disc/work separators.** Left-bar section markers (`▌ Disc 1`, indented `▏ Work`) instead of dashed rules.
- ✅ **#44 — Nicer listening history.** Modernised into aligned structured columns (title · artist · album · when · listened) matching browse/search. `when` is a relative time (`just now`/`40m ago`/`3d ago`/`2w ago`, falling back to a date for old entries); `listened` is a readable duration (`4m 19s`); filename is the title fallback, and `Unknown Artist`/`Unknown Album` are blanked. Header shows the recent count.
- ✅ **Regular library-cache refresh.** The background sync (`_sync_worker`, every 30 s) now **always runs** — it was previously started only when an iTunes XML existed (`if xml_db:` in `main._run`), so most users got no background refresh at all. It also **reconciles against the filesystem** each cycle (`_reconcile_library`): a cheap `os.walk` (no tag parsing) diffed against the known paths **adds** new files, **drops** vanished ones, and thereby picks up **external renames/moves** (remove + add) — not just mtime edits to already-known paths. **Unmount guard:** if the scan returns zero files while the cache still holds entries under the music dir (temporarily unavailable/network drive), nothing is removed, so the cache isn't wiped. In-app edits/renames still update the cache immediately via `refresh_library_entry`. Add/remove/rename/no-op/guard cases verified headlessly.
- ✅ **#68 — iTunes XML feature scrapped.** The `Library.xml` enrichment (intended for Apple Music / DRM-`m4p` compatibility) never worked, so it was removed **entirely**: `XML_PATH`, `load_xml_database`/`_parse_plist_value`/`_enrich_from_xml`/`_update_metadata_from_xml`/`_get_xml_mtime`/`normalise_string`, the `xml_db`/`xml_title_keys` params threaded through `get_metadata`/`build_library`/`refresh_library_entry`/`start_background_sync`/`_sync_worker` (and the XML-reload block), the `xml_db_path` config key, the Settings *Update/Clear iTunes Library XML Path* entries, the dead browse-side *Toggle XML* display (`get_xml_metadata_lines`/`display_xml_metadata` + `format_duration_ms`/`format_file_size`/`format_bitrate`/`format_sample_rate`), and all README/DEVELOPER doc mentions. `m4p`/`m4a`/`mp4` files still scan, play, and MP4-atom-tag as normal. pyright 0/0, imports clean, no `xml` references left in `src/` (bar unrelated iTunes tag-convention comments).

## Search

- ✅ **#8 — Clearer search results + fuzzy live search.** New `src/search.py` fuzzy matcher/ranker: tiered exact → prefix → word-boundary → substring → subsequence (fzf-style) → typo (bounded Levenshtein), scored by field weight × match geometry, with recent-play & play-count boosts; returns match spans. New `prompt.live_select` incremental widget: results re-rank on every keystroke (letters type into the query, ←→ edit, ↑↓/scroll pick, Enter opens, Esc backs out). `handle_search` rewired to it — richer rows (title · artist · album · disc/track · duration) with the **matched characters accented** in each field, and the "Edit tags — all N results" row preserved as a selectable entry. Columns: title · artist · album · **people** (its own aligned column — shows who/which role matched) · disc/track · duration. Scope is cycled in-screen with **Tab** (all → title → artist → album → genre → people; default all) — the pre-search "Search within" screen was removed. Ranking: contiguous ("solid") matches get a large band so **exact substrings always sort above fuzzy**; a relative floor (`min_ratio` 0.15) **prunes the weak tail** when a strong match exists but keeps fuzzy-only queries. People are stored as `Name (Role)` (`_extract_id3_metadata`) so roles are searchable and shown (truncating when the column is narrow) — **populates on re-scan/refresh** for existing libraries. Matcher unit-tested; row/render verified headlessly. Live widget wants a TUI pass.

## Playback

- ✅ **#13 — Queue view.** A single `w` cycles the right column: off → lyrics → queue → lyrics+credits; unavailable views are skipped.
- ✅ **#19 — Audio-general language** (not radio-specific). Player credits: CAST → PERFORMERS / PRODUCTION TEAM; `c` hint = credits.
- ✅ **#20 — Album & artist always display;** the `m` toggle governs only the extras: year, genre, and one of {work + movement | disc-or-subtitle + track | just track}. Single-disc (`x/1`) shows no disc; disc subtitle replaces "Disc N" when present.
- ✅ **#21 — Visual volume indicator.** Full-height vertical bar right of the album art (incl. split view); dim fill with a hollow "tube" for the unused section; live updates. Progress bar is white. *Gap fix:* the boundary cell used a bottom-aligned partial block whose empty top read as a gap at non-100% levels — now whole-cell fill (rounded), every cell solid `█` or tube `░`, no gaps.
- ✅ **#31 — `w` does nothing when there's no queue/lyrics/credits** (the cycle only includes available views).
- ✅ **#51 — Metadata show/hide fixed.** Artist shows even with no album (and vice-versa); extras built robustly from frame text so they appear whenever present and the toggle is on.
- 🟡 **#14 — Background playback + multi-window session.** **Design confirmed:** (1) same-process **background audio** — leave the player, music keeps going, media controls still work; (2) opening another `backtrack` window prompts **"join this session or new session?"** — join = a controller/viewer of the same session (see now-playing, pick/queue, one player *view* at a time across windows); (3) **build Phase 1 (single-window) first, then Phase 2 (multi-window IPC)**; (4) when the audio-host window closes, playback **hands off** to another attached window (music survives). Keys: **b/Esc = minimise (keep playing)**, **s = stop**, **n = next**, **q = quit app**; **Ctrl-P** reopens the player from anywhere.
  - **Phase 1 progress:**
    - ✅ **Session foundation** — new `src/playback/session.py`: a process-wide `PlaybackSession` singleton (`SESSION`) owns the reused VLC instance/player, the queue (paths/titles/index + repeat mode), and the track lifecycle; a **background daemon tick** advances the queue on track-end + logs history whether or not a view is attached; `now_playing()` snapshot for the bar; the low-level audio primitives (`_apply_equalizer`/seek/stderr) moved here. Queue-advance logic verified headlessly; pyright 0/0.
    - ✅ **Player-view + caller refactor** — `playback.py`'s `music_player` now *starts the session + attaches a view* (`open_player_view`); the loop sources the player from `SESSION`, delegates end→advance to `SESSION.tick()` (reloading lyric/handoff state on track change), and applies the new exit keys (b/Esc detach-keep-playing, s stop, n next, q quit). `menus.play_queue` no longer loops on return status — it starts the session with the full `queue_paths` + repeat mode and returns on minimise (audio keeps playing in the background). `main` calls `SESSION.shutdown()` on exit (stops audio, restores stderr). pyright 0/0. **Wants a live playback smoke test.**
    - ✅ **Now-playing box + Ctrl-P hotkey** (step 3) — a **3-line rounded box** drawn just above the breadcrumb on the widgets' `_wait_for_keypress` poll (~4 Hz), so every menu/browse screen shows background audio without per-widget wiring; `_visible_rows` reserves the box height + 1 while active, `_render_now_playing_bar` clears the band a taller previous box left behind. **Styling only (no colour):** top border, a content row (`▶`/`❚❚` icon + **bold title** · dim artist · dim-italic album, then just `m:ss / m:ss`; volume/queue-position dropped from the box), and a **bottom border that is the progress bar** (heavy `━` elapsed / light `─` remaining). **Ctrl-P** in `select()` reopens the full player over the current list and redraws on minimise, and a **`^P player` hint appears in the normal menu hint bar** (recomputed each render) while audio is active. Wired via registered callbacks (`ui_utils.set_now_playing_provider` returning `list[str]` / `prompt.set_player_opener`) so `prompt_core`/`prompt` never import the playback layer. Also fixed: Escape-in-player (`terminal_input` emits a lone `\x1b`) and a latent `_clip_ansi_to_width` bug (it mistook `[` for the CSI terminator, corrupting any truncated styled line). pyright 0/0; box rendering verified headlessly at multiple widths (rounded box, progress fill, truncation, too-narrow fallback). **Wants a live pass.**
    - ✅ **Queue actions** (step 4) — `SESSION.enqueue` (append) + `SESSION.play_next` (insert after the current index). All three track action menus (history/search/browse) now offer **"Play next"** and **"Add to queue"** — shown only while a session is active (via shared `_queue_action_choices()` / `_handle_queue_action()` helpers), with a status toast. Insert/append logic verified headlessly; pyright 0/0.
  - **✅ Phase 1 complete** (single-window background audio) — wants a full live pass.
  - 🟡 **Phase 2 — multi-window shared session.** **Design confirmed:** per-session Unix sockets + a discovery **registry** so **multiple joinable sessions** can coexist (launch chooser: New / Join ⟨each live session, labelled with its now-playing⟩); host-close **hands off** (new host resumes the current track near its last position, ~1s gap ok); build in sub-stages **2a→2d**, checkpointing each. Socket transport verified working (AF_UNIX, `~/.config/backtrack/sessions/`).
    - ✅ **2a-foundation — IPC core** (`src/playback/ipc.py`, new, pure transport + discovery, no playback logic): newline-JSON protocol; `SessionServer` (binds a per-session socket, writes a registry JSON with a label + now-playing summary, accepts clients, broadcasts snapshots ~4 Hz, forwards client commands to a handler); `SessionClient` (connects, mirrors snapshots, sends commands, fires on-disconnect); `list_sessions()` discovery that probes liveness and cleans up crashed/stale entries. **14/14 headless checks** (advertise → discover → mirror + live updates → command client→host → disconnect detect → discovery empties → stale cleanup); pyright 0/0.
    - ✅ **2a-wiring** — host `PlaybackSession._ensure_advertised()` starts a `SessionServer` when it first plays (unless this window is itself a client); a host `_handle_remote_command` dispatcher is wired for 2b. Launch flow `main._maybe_join_session()` runs discovery and shows the **New / Join ⟨label — ▶ now-playing⟩ chooser**; joining sets a module-level **client link** (`session.set_client_link`/`is_client`/`current_now_playing`). The now-playing box reads `current_now_playing()` so a joined window **mirrors the remote host read-only**; `music_player` refuses local playback while joined (a status nudge; remote control is 2b). Shutdown stops the server / closes the client link. pyright 0/0; role-switch + mirror + box-rendering verified headlessly (8/8). **Live two-terminal join confirmed working.** **Flicker fix:** the box was wiped by each list redraw's `\033[J` (anchor→end-of-screen) and only restored on the 0.25 s idle tick, so it strobed during navigation — now `_Widget.render` redraws the box **atomically in its own flush** (`now_playing_box_segment`) and the idle tick redraws **only when the box content changed**.
    - ✅ **2b — remote control.** `session.RemoteSession` — a client-side proxy presenting the same control surface as `PlaybackSession` but forwarding each action (`play`/`enqueue`/`play_next`/`pause`/`next`/`prev`/`seek`/`set_volume`/`stop`) to the host over the IPC link (fire-and-forget; state returns via the next mirrored snapshot). `session.active_session()` returns the local `SESSION` when hosting or the proxy when joined, so the menus/queue-actions/`music_player` drive playback identically in both roles: **picking a song or queueing in a joined window now plays on the host** (`music_player` sends `play` in client mode instead of opening a local view). Host applies commands via `_handle_remote_command`. pyright 0/0; command-forwarding + role-switch verified headlessly (8/8). **Wants a live two-terminal pass** (join → pick/queue → host plays).
    - ✅ **2c — view lock + client player view.** **One player view at a time across windows:** each window has a per-process token (`session.my_token()`), the session tracks a `view_holder` (broadcast in the now-playing snapshot), and `acquire_view`/`release_view` (local + over IPC via `_handle_remote_command`) enforce it — `open_player_view` (host) claims/releases the lock (detaches instead of opening if another window holds it), and **Ctrl-P** (`main._open_player`) is role-+lock-aware (refuses with "open in another window" if held elsewhere). **Client player view** (`playback.open_client_player_view`): a joined window's `Ctrl-P` opens the full player, rendering the host's current track from its snapshots + the **track file read from the shared disk** (art + metadata + progress + volume), with all transport (space/seek/next/volume/stop/minimise) routed to the host via the proxy; **elapsed is interpolated** between ~4 Hz snapshots (using `latest_at()`) for a smooth progress bar. Lock logic verified headlessly (10/10); pyright 0/0. *Lyrics on a client view deferred; wants a live two-terminal pass.*
    - ✅ **2d — host handoff.** One path covers both graceful quit and crash: the snapshot now carries the **full session state** (queue/titles/mode/index/elapsed/paused), so when a joined window's `SessionClient` sees the host disconnect it runs `session.attempt_handoff()` — an **election via an `O_EXCL` lock file** (`sessions/<id>.host.lock`): the single winner stops being a client and **re-hosts on the same socket id** (`_session_id` reuse) by reconstructing the queue and resuming the current track at its last position (`seek_to`, ~1 s tolerance as agreed); losers **reconnect** to the new host (retry until it rebinds). Wired to the client link's `on_disconnect`. Election exactly-one-winner + full-state snapshot verified headlessly; pyright 0/0. **The concurrency-heavy piece — wants a live multi-terminal kill-the-host test.**
  - **✅ Phase 2 code-complete** (2a–2d). **#14 is feature-complete pending live multi-terminal validation** (join/mirror ✓ live so far; 2b/2c/2d want two-plus terminals sharing a `CONFIG_DIR`).
  - **Refinements (post-live-feedback):** (1) **queue-when-idle** — the track menus now offer *Play next*/*Add to queue* whenever you're a joined window (not just when something's playing), and queueing onto an **idle** session **starts playback** with that track, so the now-playing box/panel appear and `Ctrl-P` works (before, an idle host left you unable to open the panel). (2) **live queue pane** — the host player view now refreshes the queue pane whenever the queue changes mid-track (e.g. another window queued a song), not only on track change. Verified headlessly (6/6). *(The client player view still has no queue/lyrics pane — that + client-side lyrics are the remaining 2c follow-up.)* (3) **first-track title showed the filename** — a single-track play (`music_player` with no `queue_titles`) fell back to `os.path.basename(path)` for its queue title, so the first entry showed the file name while later-added tracks (which carry the menu's title) were fine; now `SESSION._title_for()` derives the display title from the track's **ID3 TIT2** (fallback filename-no-ext) in `start`/`enqueue`/`play_next`. Verified headlessly. (4) **now-playing box frozen until focus + keystroke** (esp. multi-window: a track advancing in the *player* window left the *browse* window's box — clock included — stale until it was focused and got a keypress, even a no-op one). Root cause: the box repaint rode **solely** on the menu's own input poll, so a passive/mirror window (a joined client, or a host playing in the background) only repainted when the user drove a render. Fix — **event-driven repaint via a self-pipe**: background threads call `ui_utils.pulse_now_playing()` on a state change and the menu poll's `select()` now watches a wake pipe alongside stdin, repainting the box **immediately** (~150 ms in tests) instead of on the next keystroke/timeout. Pulse sources: a joined window's snapshot receiver (`SessionClient.on_snapshot`) and the host's auto-advance tick loop (`_tick_loop`, while active-and-unattached — covers the clock too). A wake is *not* treated as a keypress (`select` distinguishes the fds); the pipe is non-blocking + coalesced so pulses never block a background thread. Belt-and-suspenders: the idle-tick redraw also keys off a cheap **track identity** (`file/generation/paused/index/count/view_holder`) so an auto-advance repaints even if two tracks render byte-identical rows (`generation` bumps every `_load`); poll backup tightened 0.25 s → 0.12 s; anti-flicker no-change suppression preserved. Verified headlessly end-to-end (host track-change → broadcast → client box repaint with **zero keystrokes**, under a deliberately long poll timeout; wake ≠ keypress; no-flicker suppression intact). **Wants a live two-terminal confirm.** (4b) **box still stale after a window loses focus, until clicked** — even with the pulse, an *unfocused* window's box would go stale and only refresh on a click. Root cause: while a window is unfocused the terminal often doesn't paint our box writes, yet the process's box cache *advances* as if it had, so on refocus the change-detection sees "cache already matches current" and suppresses the repaint — leaving pre-unfocus content until a full render (the click). Fix: on **focus-in** (`\033[I`, detected in `_read_key_raw`) call `invalidate_now_playing_box()` (drops the line/sig cache + zeroes the poll throttle) so the next idle tick repaints unconditionally; the `select` loop additionally does a full `anchor_reset()`+render on `FOCUS_IN` in case the terminal blanked the whole window. Now a window refreshes the instant it regains focus, no click needed. Verified headlessly: reproduced the stale-suppression (idle tick skips after an unfocused write), confirmed focus-in invalidation forces the repaint to the current track, and confirmed `FOCUS_IN` in a `select` key stream repaints without exiting/mis-selecting (row actions + Enter still work); pyright 0/0. (4c) **hide the ambient bar while the full player is open anywhere in the session** — when any window holds the player view, the player itself *is* the now-playing display, so the small bar in every other window would just double it up. `format_now_playing_bar` now returns None whenever `now_playing()['view_holder']` is set (it's the token of whichever window holds the view, broadcast to joined windows), so the bar vanishes session-wide while a player view is open and returns when it closes. To keep the menu layout correct across the show/hide, the idle tick flags a height change (`ui_utils.mark_now_playing_layout_dirty()`) that `consume_resize()` now also reports, so every menu re-reserves/reclaims the bar's rows on the transition (no list/box overlap). Side effect: the `^P player` hint also hides while the player is held elsewhere — correct, since `^P` there just says "open in another window." Verified headlessly: bar shows with no view_holder, hides when set; 0→3 and 3→0 transitions flag layout-dirty (menus re-render), steady state doesn't; pyright 0/0. (6) **a joining window grabs the player if it's free** — the player view is single-instance across the session (the view lock), so on join `main._take_player_if_free()` checks the session: if it's playing and **no** window currently holds the view (`view_holder` is None), the new window **auto-opens the client player view and locks it** — the existing window then can't open the player (`_open_player`'s guard → "open in another window"), and symmetrically, if the existing window already holds the view, the joiner skips the auto-open and likewise can't take it until the holder leaves. Idle sessions and already-taken views are left alone (no wasted wait when the registry shows idle; otherwise it waits ≤0.5 s for the first mirrored snapshot to read the freshest `view_holder`). `QUIT_ALL` from the auto-opened view propagates as `QuitToTerminal`. Verified headlessly: idle→skip, active+free→open, active+held→skip; pyright 0/0. **Wants a live two-window confirm** (open window A playing in the background, launch window B → B should jump straight into the player and lock A out of it, and closing B's player should hand the ability back). (7) **the player window is pinned while a browse window exists** — you can't minimise/leave the player view back to browse (`b`/`Esc`) while another window is part of the session, so the two windows stay specialised (one browse, one player) until one closes. New `session.has_other_windows()` (True when we're a joined client with a live link, or a host with ≥1 connected client via `SessionServer.peer_count()`) guards the `b`/`Esc` DETACH in **both** player views (host `_player_view_loop` and client `open_client_player_view`); when blocked it flashes "Close the other window to leave the player" and stays put. `s` (stop) and `q` (quit the window) still work — they're the intended way to collapse back to one window; only the keep-playing minimise is pinned. Verified headlessly: `has_other_windows` True for connected client / host-with-peers, False standalone / 0-peers / disconnected; pyright 0/0. **Live two-window confirm wanted.** (7b) **never two player views at once** — `open_client_player_view` acquired the view lock but never *checked* it, so a client could open a second player on top of a window that already held one. It now refuses (returns DETACH + "open in another window") when `now_playing()['view_holder']` is already another window's token, and also bails mid-loop if another window wins a simultaneous grab or takes the view (`view_holder not in (None, my_token)`). The host path (`open_player_view`) already honoured its atomic `acquire_view`; this closes the client gap so all open paths (auto-open on join, Ctrl-P, direct) respect the single-instance lock. Verified headlessly: held-elsewhere → DETACH without acquiring; idle → OK; pyright 0/0. (5) **queue actions are now listing-level key shortcuts** (not track-menu items) — in the Browse and History track lists, **`n` = Play next** and **`a` = Add to queue** act on the *highlighted* row and stay in the list, so you can build a queue without drilling into each track's action menu. Implemented via a general `select()` `row_actions` (key→callback on the current row, like `on_inspect` but any number of keys) + `row_action_hints`; menus supply them through `_queue_shortcut_kwargs(library)`, gated on `active_session().is_active() or is_client()` (same visibility as before) and ignoring the Play-all/disc/work header rows. The two actions were dropped from the Browse/History action menus. **Search results keep the menu-based actions** (they use the `live_select` widget, which doesn't take the shortcuts) — a candidate for a follow-up if you want `n`/`a` there too. Verified headlessly: gating (idle-non-client → no shortcuts), dispatch (`n`→play_next, `a`→enqueue, client-idle→start), header-row guard, and a scripted `select` run (row actions fire and stay, Enter still selects, hint bar shows `n play next` / `a queue`); pyright 0/0.
- ✅ **#47 — Hide the "Edit Lyrics" action** for tracks with no lyrics at all (gated on `find_lyrics()` in search, history, and browse).
- ✅ **#50 — Low volume.** Confirmed fine on a live audio test — low levels are quiet-but-audible (only 0% is silent). No perceptual/log remap of `audio_set_volume` needed after all.
- ✅ **#57 — Centre/balance the queue view** (the block is now horizontally centred within the pane).
- ✅ **#80 — Hints must never be cut off.** `ctrl_row` clamped to `rows - len(shortcut_lines) - 1` in all four playback rendering branches so hints always stay within the terminal. **Follow-up (toggle-help layout fix):** in **wide** mode (both no-pane and split), toggling help on drew the multi-line hints *over* the transport controls — the art was sized with a fixed chrome constant that ignored the hint-line count, so the clamp shoved `ctrl_row` up onto the art/progress. Now the wide branches size the controls/hints **first** (as the `standard` branch already did) and subtract `len(shortcut_lines)` from both the art height (`avail_h`) and the vertical-centring (`top_pad`); the transport line is recomputed after the art so it still centres correctly. Result: toggling help **shrinks the art and shifts the controls up by the same amount**, keeping the hint block's bottom fixed on-screen. Arithmetic verified headlessly across heights (art shrinks, controls move up, zero overlap, hints on-screen); wants a quick live confirm.
- ✅ **#81 — Volume bar placement + stability.** Fixed. **Two follow-ups this session:** (1) the volume **percentage label disappeared on a full redraw** (e.g. toggling help with `i`) — it sits on the art-bottom+1 row, and the spacing blank line logged just after the art erased it with `\033[K`; `+/-` masked it because `draw_volume_bar` re-ran afterward. Fixed by logging the (absolute) volume cells **after** the spacing blank in all three draw branches, so nothing erases the label. (2) The bar could **hang below a shortened art** when the layout was tight (art flow lines are clipped at `rows − MARGIN_V`, but the absolute bar cells weren't) — `_volume_bar_cells` now clamps the bar height to that same limit, so the **bar bottom always lines up with the art bottom**. Both verified headlessly (label survives the blank; bar clamps to the visible limit).
- ✅ **#82 — Metadata reliably on-screen in playback.** Fixed. **Follow-up:** metadata (title/artist/album) was drawing *above* the art in standard/minimal (narrow) layouts. Cause: `_ABS_ROW_RE` (`^\033[\d+;`) also matched the 24-bit colour prefix (`\033[38;2;r;g;bm`) every half-block art line starts with, so `_render_frame_buffer` treated art lines as absolute (row-less) and the flow metadata took the top rows. Tightened to `^\033[\d+;\d+H` (true cursor positioning only); wide mode was unaffected because its art lines are space-prefixed. Art now renders above metadata at all widths (verified headlessly).
- ✅ **#86 — Help hints toggle only on `i` and persist.** `update_ctrl_ui` was rewriting the hint line(s) on every play/pause/seek/volume action, so they flickered/reappeared. It now redraws only the transport/status line; the hint lines are owned solely by the full-UI draw (start, `i`, resize, focus, panel/meta toggles), so they stay put and change only when `i` is pressed.
- ✅ **Album art reloads after an edit.** The playback art cache (`_get_art_cached`) is keyed on the file's mtime, so adding/replacing album art (or any tag edit) shows on the next play instead of a stale "No album art found." until restart; the old-mtime entry is evicted so repeated edits don't grow the cache.
- ✅ **Multi-disc cover detection.** `cover_matcher.find_images` now also scans the **album folder one level up** when the track's directory is a per-disc subfolder (`_is_disc_dir`: `CD1`/`Disc 2`/`DVD1`/`Vol. 3`/`Part 1`/`Side 1`…), so a cover shared across discs (`The Wall/Cover.jpg` with tracks in `The Wall/CD1`) is found for every track — previously only the track's own folder + its artwork subfolders were scanned, so an album-level cover on a multi-disc release was invisible to the picker and the bulk op. A flat album is unaffected (it never reaches up into the artist folder). Whole-album cover names still score 'low' by design (one shared cover shouldn't out-rank a per-track match). Two follow-ons so a shared cover is easy to **apply to all**: (a) **Auto-detect now assigns a lone album cover to every track** — when nothing pairs per-track and there's a single obvious cover (the only image, or the one front-cover-named `_sole_album_cover`), `plan_best` puts it on all tracks (checked by default) instead of leaving every row "— none —"; per-track albums are untouched (only fires when per-track finds nothing). (b) **picking a cover with `d` offers "Just this track / All N tracks"** in the preview, so any cover (incl. a low-confidence shared one) can be pushed to every row in one step. NB playback still shows only **embedded** art, so embed via the op, then it renders on play. Verified headlessly (parent `Cover.jpg` detected for a `CD1` track; flat album doesn't pull the artist-folder image; Auto assigns the shared cover to all 3 CD1 tracks; per-track album still pairs 1:1; `_sole_album_cover` lone/front/ambiguous cases); pyright 0/0.
- 🟡 **#87 — TEMP: `e` skips to the last 35 s** of the track (marked `# TEMP` in `playback.py`/`playback_ui.py`). **Keeping it for now** (intentional — do not strip as cleanup); to revert later, remove the `elif key.lower() == 'e'` block + the one hint tuple.

## Metadata & tag editor

- ✅ **#15 — Bulk uses the specific value prompts.** Bulk "Add New Tag" reuses `prompt_for_value` like single-track editing.
- ✅ **#17 — Bulk header box spans the full width.**
- ✅ **#27 — Add Tag is a shortcut** (`a`), not a list button.
- ✅ **#54 — Friendly names fill the column then truncate** (single-track). Removed the hardcoded 22-char cap; the column sizes to the longest name (bounded to the window) and truncates per row keeping the `)`.
- ✅ **#75 — Single-track editor mirrors the bulk layout.** 4 columns: `TAG (friendly name) · type | value`, with friendly name + type dimmed; full-width rounded box header.
- ✅ **#76 — Editor header uses real tags,** not the filename: `title · artist` + duration + `[format]`/size.
- ✅ **#16 — Audit every tag's input method.** Every frame now routes to a fitting widget. **Numeric/rating pass:** `prompt.rating_edit` — POPM star editor (0–5★ ⇄ 0–255 **Windows Media Player** scale `0/1/64/128/196/255`, nearest-on-read) with play count + rater email; `prompt.number_edit` — bounded-int spinner (↑↓ ±1, PgUp/PgDn ±10, type digits, Ctrl-T→raw). Wired by base id: `POPM`→rating, `PCNT`/`RBUF`→number, `INT_BIG` (TBPM/TLEN/TDLY)→number. **Enum/bool pass:** `TKEY`→musical-key picker (majors/minors/off-key + custom), `TMED`→media-type picker (CD/DIG/VIN/… + custom), `TSRC`→ISRC field (normalised + `CC-RRR-YY-NNNNN` shape-check), `TCMP`→Yes/No toggle; `RVRB` reports "not editable" (structured, no simple form) instead of a broken text box. Registry: TKEY/TMED/TCMP re-flagged `single_only` (they were wrongly multi). `create_frame` + `summarize_tag_value` gained POPM/PCNT/RBUF/TCMP branches — POPM/PCNT/RBUF were previously *uneditable* (create_frame returned None). All create→save→read→summarize round-trips + ISRC validation verified headlessly (18/18); pyright 0/0. *Interactive pickers want a live TUI pass.*
- ✅ **#53 — Structured columns + merged select/checkbox (no parsing).** `select(columns=[Column(...)], multiple=False)` + each `Choice(cells=[...])`; the renderer lays out/styles per-column with zero string parsing. `checkbox()` is now a thin wrapper over `select(multiple=True)` (one widget, shared layout/keys/clip/focus). Tag keys like `APIC:Front Cover` and values like `3/12` can never be misparsed; the bulk picker uses cells too (old title parser deleted). A cell can be a string, a `(text, style)` tuple, or a **list of segments** so one column can mix styles — the tag/bulk column 1 holds `TAG` (bright) + ` (friendly)` (dim) together.
- ✅ **#36 — File-path row styling.** "⌁ File path" is white inactive, white+bold active; the `(filesystem)` hint is dimmed (via the columnar friendly-name styling).
- ✅ **#37 — In active rows, bold and dim the friendly name** but not the tag type (both column renderers).
- ✅ **#38 — "Import list from text"** now pre-fills a format template in the editor; `f` key imports directly from a file path.
- ✅ **#39 — Hollow cursor when the window is unfocused.** Focus reporting (`\033[?1004h`) enabled app-wide; the in-place field editors (list/fraction/time/date-time) draw a solid `█` when focused, a hollow `▢` when not. *Wants a live check — focus events can't be tested headlessly.*
- ✅ **#40 — System editor.** `_find_editor()` tries `$EDITOR` first, then probes micro/nano/vim/vi/emacs via `shutil.which`; clear error if nothing found.
- ✅ **#42 — Smart sort-order default.** When adding a sort tag (TSOT/TSOA/TSOP/TSO2/TSOC), prefills from the matching source tag and generates ranked candidates via a multi-pass **pure-heuristic** engine: merges space-separated initials (J. S. → J.S.), merges Celtic joining prefixes (Mc/Mac/O'), strips leading honorifics (Dr./Prof./Sir/MC/DJ/Lady/Lord/…) and trailing suffixes (Jr./III), detects spacing surname prefixes (von/van/de/…), and offers all right-splits **last-word-as-surname first** (the positional default). Extends article stripping to French/Spanish/German/Italian/Dutch. Pads single-digit ordinals (Series 1 → Series 01). Multi-artist strings (split on &/feat./and/with/|/+/… — but not "and his/her/their" which describes a backing ensemble) sort each entity and rejoin with a configurable delimiter (Settings → EDITORS → Sort List Delimiter, default /). A simple name (one/two words, or a leading article) resolves to a single candidate and prefills silently; an ambiguous multi-word name shows all candidates via select; "type custom" always available. `MC X`/`DJ X`/`Dr. X` sort as `X, MC`/`X, DJ`/`X, Dr.`; `Artist and His Band` doesn't split on `and his/her/their`. **Corpus removed (this session):** the JSON name corpus (`name_corpus.py` + `corpus_full.json`/`corpus_minimal.json`, ~9k given names / compound-surname / mononym lists) and the MusicBrainz validation pipeline were **deleted** — the engine is now fully automatic from patterns, and anything it gets wrong (a band inverted as `Punk, Daft`, an unusual split) is fixed by the user picking a different candidate or "type custom". Honorifics/suffixes are now small inline `frozenset`s in `id3_browser.py`. The `name_corpus` config key + Settings "Name Corpus Size" entry are gone. Heuristic output verified headlessly across Bach/Beethoven/McCartney/Dr. Dre/MC Solaar/Mozart/etc.
- ✅ **#60 — Multi-value tags.** Text frames the registry doesn't flag `single_only` now hold multiple values (à la Picard). The `single_only` flag in `tag_registry.py` is the gate; **TCON (genre), TCOM (composer), TPE1 (artist)** were relaxed to multi this session on top of the existing 42 candidates (conductor, remixer, lyricist, mood, key, language, TXXX, …). **Editor (avenue A):** `prompt_for_value` reads *all* `.text` values and, for a multi-value frame, opens a **simple single-line text field by default** — **`Ctrl-T` expands to the structured list editor** (`list_edit`: add `a` / edit `e` / delete `d` / reorder `K`/`J` / import `i`/`f`, single-column `num_cols==1`). It **auto-opens the list** when the frame already holds 2+ values (a single field can't show them without hiding data). A half-typed value is carried across the toggle (`prompt._toggle_carry`) so expanding from the field doesn't lose the edit; the `^t` hint reads `values` for these frames. The toggle is only enabled where it's meaningful (multi-value, people, date/fraction) — plain-text/URL frames like TALB no longer show a `^t` hint at all. **Write:** `create_frame` accepts a `list[str]` for TEXT and text-LIST frames (genre/language) — people frames still branch on `ui_category=='people'`. **Display:** `summarize_tag_value` and `_extract_id3_metadata` join values with `'; '` (single-value output unchanged — one element). **ID3 version:** v2.3 collapses multi-value text into one `/`-joined string (corrupting `/`-containing values), so a new `save_id3()` helper saves **v2.4 iff any frame holds >1 value**, else v2.3 (max player compatibility); all ID3 save sites route through it so a later single-value op can't collapse existing multi-values. Round-trip (incl. slash values), version selection, widget dispatch, and plain-text parsing verified headlessly. *Wants a live TUI pass on the list editor for multi-value.* MP3/ID3 only (no MP4 freeform atoms).
- ✅ **#61 — Copy tag across tracks.** "Copy from first track" bulk operation reads source frames from track 1 and deep-copies them to all other tracks.
- ✅ **#84 — Bulk people-list editing + reorder.** Bulk-editing a TMCL/TIPL tag (Set value) now opens a per-tag **common-entry editor** instead of a blind whole-list replace: it aggregates the tag's distinct `role → name` entries across the selected MP3s with an `N/total` coverage count, and **edit** (`e`/Enter → submenu) replaces that exact pair in every file that has it, **remove** deletes it from all that have it, **add** (`a`) appends to every file lacking it — per-file order preserved (edits replace in place, `_people_apply` de-dupes). Pure transform unit-tested + TMCL round-trip verified; MP3-only (no MP4 people atoms). **Individual reordering**: `list_edit` gained move-up/down (`K`/`J`, shown in the hint bar), so the single-track people editor (and any list editor) can reorder entries. **Import-from-file** (`f`) fixed: uses the `path()` prompt (completion) instead of a raw `input()`, and a new `_parse_import_rows` auto-detects the layout — CSV (quoted fields via the `csv` module), TSV, `;`/`|`/`:`/` - ` separators, or run-of-spaces columns — skipping comment/blank lines and a repeated header row (was colon-only splitting before).
- ✅ **#63 — Logical default friendly-name order.** Reordered: `TOPE`/`TOLY` drop `(s)` suffix first; `TKEY`→"Key"; `TLAN`→"Language"; `TDRC`→"Year"; date timestamps say "date" not "time"; `TSSE`→"Encoder"; `WOAR`→"Artist URL".
- ✅ **#83 — Derive tags from filename & folder structure.** New bulk operation "Derive from filename" (first slice of the pattern-based idea below). **Parsing** (`src/id3/filename_parser.py`, pure + unit-tested): track/disc + totals from `01` / `1-05` / `SxxExx` / `1x05` / vinyl `A1` / `Track N` (4-digit years guarded out); title → TIT2 (`_`→space, whitespace collapsed, a leading `Artist - ` stripped/extracted **only** when it's a compilation or the album artist appears in the prefix — so `Interlude - Reprise` stays a title); album + album-artist from the `Artist/Album` chain, where `CD/Disc/Disk/Series/Season` subfolders collapse into the album and set the disc (filename `2-05` wins on conflict); disc **subtitle** → TSST from `Disc 2 - The Extras`, and a bare `Series 1`/`Season 2` becomes the subtitle itself (so playback shows "Series 1"); **Various-Artists compilation** detection → TPE2 = "Various Artists" + TCMP/`cpil`; **year/date** → TDRC from `Album (1997)` / `1997 - Album` folders or a filename `1952-02-20`; `Artist - Album (Year)` single-folder fallback; **smart sort orders** (TSOP/TSO2/TSOA/TSOT) via the #42 engine. **Writing** (`src/id3/tag_writer.py`): MP3 (creates a fresh ID3 for blank files) **and** MP4 atoms natively (`©nam`/`aART`/`trkn`/`disk`/`cpil`/`soar`…); fill-blanks default with overwrite opt-in; MP4 disc-subtitle has no atom so it's skipped and *said so* (not silent). **UI**: field toggles (title pre-ticked), auto-detect **or** a Picard-style `%token%` template override (`%track% %disc% %title% %artist% %albumartist% %album% %year% %date% %season% %episode% %ignore%`), and an overflow-safe preview — fields identical across all files lift into the header, only *varying* fields become columns, and `d` opens a full untruncated per-file detail view (new additive `select(on_inspect=, inspect_key=)`). Hardened the existing bulk ops too: non-MP3s skip quietly, untagged MP3s no longer raise (Add New Tag creates a header).

## Dates & equaliser editors

- ✅ **#74 — Apply stored EQ during playback** via libvlc's equaliser (auto-applied on load; brief toast; each EQU2 point snaps to the nearest libvlc band so untouched bands stay flat).
- ✅ **#79 — RVA2 dB editor** alongside the EQU2 graphic EQ (part of #16).
- ✅ **#56 — Robust ISO8601 dates + timezones.** `datetime_edit` now has a third Timezone section (TAB cycles date→time→tz); opens `timezone_select()` — a full-screen world map picker rendered in Unicode half-blocks (▀▄█) at 72×18 display resolution from a 72×36 land/sea bitmap, with equator (`─`) and prime meridian (`│`) overlaid. 95-entry timezone table covering UTC-12 to UTC+14 (all fractional offsets); left/right arrows cycle offsets, up/down cycle zones within offset, type to search. Returns ISO 8601 offset (`+05:30`, `-08:00`, `Z`).
- ✅ **#59 — More EQ presets.** Expanded from 6 to 24: Rock, Pop, Jazz, Classical, Electronic, Hip-hop, R&B, Acoustic, Dance, Country, Metal, Folk, Latin, Speech, Bass cut, Treble cut, Piano, Night mode.

## Lyrics

- ✅ **#35 — Lyric editor: mouse + duplication.** Mouse enabled (scroll navigates, click positions the cursor on a row); every line is clipped to width so long lines no longer wrap and ghost each frame. *Click-to-row is best-effort by layout — wants a live check.*
- ✅ **#29 — Append writing credits.** `c` key in lyrics editor (SEG mode) appends TCOM (Music by) and TEXT (Words by) credit lines as untimed segments at the bottom of the lyric list.
- ✅ **#47 — (see Playback)** hide Edit Lyrics when there are no lyrics.
- ✅ **#66 — Removed `lyric_timer.py`.** The only used function (`save_sylt_entries`) moved into `lyrics.py`; the dead interactive sync tool deleted; both importers updated.
- ⬜ **#67 — Lyrics-format doc** (an md on how lyrics files should be formatted).
- ⬜ **#69 — More lyric imports.** SYLT from json+uslt or json+md; USLT from txt/rtf/md, etc.

## Layout & rendering

- ✅ **Background-activity notice: persists + pulses.** The library-sync notice ("Checking library for updates…" / "Syncing library (i/N)") is a persistent `BACKGROUND_TASKS` entry, but the status bar only repainted on interaction, so it wasn't live and the cyan `●` was static. The menu idle tick (`_wait_for_keypress`, ~8 Hz) now re-stamps the status bar whenever a task is active (and once more after the last clears), so the notice stays up for the whole job and the beacon **pulses** (`ui_utils._pulse_circle`, a 256-colour cyan brightness ramp `23→51→30` keyed on `time.time()`). `ui_utils.has_background_tasks()` gates it so an idle screen with no activity isn't redrawn. Verified headlessly (message persists, `38;5;` pulse frames vary over time, one clearing redraw after done, none when idle); pyright 0/0.
- ✅ **Activity centre (live current-activities panel).** `menus.notification_centre()` — a live full-screen panel listing every running `BACKGROUND_TASKS` entry with its status + a pulsing `ui_utils.pulse_circle()` dot, a "N running" count, and a placeholder when nothing's active; it polls + re-keys on the pulse frame so it animates and updates as jobs start/finish, and closes on Esc/b/q. Opens **two ways** (as chosen): a **Settings → "Activity Centre"** entry, and **clicking the status-bar ● beacon** (a click on the status-bar row while a task is running, via `prompt.set_notification_opener` + a status-row hit-test in `select`'s mouse handler; `main._wire_playback` registers the opener). Scope is **live activities only** (no history buffer). Verified headlessly (empty → placeholder + header; active → lists all tasks + "N running" + pulse dots; closes on key; opener wired); pyright 0/0.

- ✅ **#77 — Lists never wrap at any width.** ANSI-aware clipping in select/checkbox/EQ; the tag list reflows column widths on resize; fixed a per-render file read that made resizing sluggish. **Vertical fill fixed:** lists showed a premature "N more" (~5–7 rows early) — `_visible_rows()` pre-reserved header/hint/chrome space AND each widget subtracted its own header+hints again (double-count). `_visible_rows()` now returns the true output budget (`rows − 1 − 2·MARGIN_V`) and each widget subtracts only its exact non-item lines; select/live_select/list_edit now fill the window precisely (verified across heights, no bottom-clipping).
- 🟡 **#32 — Everything looks good at any window size.** Lists no longer wrap (see #77); broader per-screen polish ongoing (see #33, #34, #57).
- ✅ **#4 — Global margins.** Even left+right spacing (`MARGIN_H`, 2 cols each side) on terminals with zero margin. The invariant: a framed row's printable width = `get_terminal_width() - 2*MARGIN_H`, left-prefixed by `MARGIN_H` → symmetric 2/2. The select/checkbox/live_select lists and the list-edit table already honoured this (`_cols()`); the **two rounded box headers** (`bulk_id3_manager._bulk_header`, `id3_browser` editor header) were the bug — `inner` reserved only the left margin (`cols - mh - 4`), so the box spanned edge-to-edge with no right padding. Fixed to `cols - 2*mh - 4`. The `text()`/`path()` input frames were left-2/right-4; evened to 2/2 (`content_width`/`max_w` = `cols - 4`). **Exempt (unchanged):** horizontal rules/dividers span full width, and album art keeps its own narrow-window centring (min-`MARGIN_H` floor, may bleed wider). Margin math verified even across widths 60–137 headlessly. **Follow-up:** the bottom hint block in `select`/`live_select`/`list_edit` hugged the left edge when wide — `_hint` centres within `_cols()` (= width−2·MARGIN_H) but those three appended the result at column 0 (other callers already prefix a margin). Now prefixed with `MARGIN_H`, so hints are symmetric and never touch an edge; the adaptive engine still reshapes (long line → pyramid/grid/stack) before the content would exceed the inset width.
- ⬜ **#30 — Fix boundary window-size duplication.**
- ✅ **#71 — Album art rendering (in-project half-blocks).** The `viu` dependency was dropped — art is computed in-project by `render_native_half_block` (cv2 → ANSI half-blocks) in `src/art/album_art.py`; `render_album_art`/`get_art`/`_convert_apic_to_viu` (legacy name) all route through it, no external binary. Layout bugs around it were fixed separately (metadata-above-art #82, margins #4). *Wants a live visual pass for fidelity/spacing (half-block downsampling); the `viu`-named helpers and the `ART_MAX_WIDTH` comment are legacy naming, and the `subprocess open/xdg-open` path is the separate "open in external viewer" feature.*

## Settings & power-user options

- ✅ **#2 — Lead-in time UI** prefilled with the current value, validated, clamped ≥0.
- ✅ **#3 — Tidy "Clear History".** Shows the entry count, no-ops when empty, single-line status.
- ✅ **#5 — Settings stay on the current item** after a change (`select(index=)`).
- ⛔ **#6 — Clear iTunes XML path** — obsolete; the whole iTunes XML feature was scrapped (see #68).
- ✅ **#7 — Sectioned settings** (PLAYBACK / LIBRARY / EDITORS / HISTORY via `separator()`).
- ⬜ **#18 — POST-DEV:** make default options suit a general userbase, not my testing.
- ✅ **#43 — Re-add the "preferred friendly name per tag" setting.** Settings → EDITORS → Tag Name Preferences: all registry tags shown, edit the PREFERRED NAME column via a barrel selector (cycles through registry aliases) or free-text; barrel auto-skips to text mode when there's only one alias or the current value isn't in the list; backspace on empty buffer returns to barrel (2+ aliases only). First column (TAG ID) is locked/read-only. Changes saved per-tag; toast shows count of changed entries. Bug fixed: `handle_settings` was overwriting the saved prefs with its stale config copy — now reloads after the sub-handler returns. Fallback fixed: `get_preferred_tag_name` now falls back to `info.name[0]` (was returning the raw tag ID).
- 🟡 **#62 — Lean into power users.** ✅ **Plain-text editing** — a `plain_text_editing` config flag (Settings → EDITORS → *Toggle Plain-text Editing*, default off) makes `prompt_for_value` edit values as raw text instead of the smart widgets: single-line `text()` for dates (ISO8601/DDMM/YYYY/HHMM), fractions (`n/total`), ints, lists, and plain text; the system editor (one `role: name` per line, parsed via `_parse_import_rows`) for people. Binary/asset frames (image/SYLT/EQU2/RVA2) keep their editors — raw text isn't meaningful there. ✅ **Per-edit inline toggle** — **Ctrl-T** flips the *current* value edit between the smart widget and raw text (both directions); the value widgets (text/list_edit/datetime/calendar/time/fraction) return a `MODE_TOGGLE` sentinel that `prompt_for_value` loops on (`prompt._value_toggle_enabled` gates it so it's inert elsewhere), and it's advertised in the hint bar. ✅ **Extra default-behaviour setting** — `autoplay_on_select` (Settings → PLAYBACK → *Toggle Auto-play on Select*, default off): selecting a track in browse/search/history plays it immediately, skipping the Play/Edit action menu (and the browse post-play break honours it so multi-action tracks don't loop). Toggle loop, mode flips, and the autoplay helper verified headlessly.
- ⬜ **#64 — Theming** (NOT URGENT). Accent/background colours; option to derive the playback background from the album art.

## Docs & project hygiene

- ✅ **#45 — Pyright/Pylance clean.** `pyright src` reports 0 errors / 0 warnings (was 51); cascade fixed by re-annotating `select() -> Any` plus targeted fixes and library `# type: ignore` markers. Re-run before commits.
- ✅ **#46 — Better non-mp3 error handling.** The tag editor no longer `print()`/`input()`s on failure: untagged MP3s open a fresh ID3 (so tags can be added), non-MP3s show "tag editing is MP3-only", and OS errors show a clean status. **Hardened:** `inspect_tag_loop` now refuses non-`.mp3` files with the friendly message **up front** (before any load), instead of only catching `ID3NoHeaderError` mid-loop — this also blocks an mp4/m4a carrying a stray ID3 header, which previously slipped past the per-load check and crashed downstream (`UnboundLocalError`). One clean early return; the internal `ID3NoHeaderError` branch now only handles the untagged-MP3 fresh-start.
- ✅ **#48 — Comprehensive docs + README.** Four cross-linked docs: **`docs/library-layout.md`** (parser reference — every folder/naming convention #83 recognises, worked examples from real parser output); **`docs/tag-etiquette.md`** (new — Part 1 general good ID3 practice, Part 2 how Backtrack reads/edits/auto-generates *each* tag, grounded in the real tag registry: identity, people/credits, classical, dates, numbering, genre/mood, POPM rating, sort orders, assets, MP3-vs-MP4); **`docs/filesystem-etiquette.md`** (new — comprehensive library-organisation good practice: album-artist grouping, album/track/multi-disc/compilation/classical/episodic naming, cover-art files, cross-platform character hygiene, keeping the library clean, letting the bulk tools tidy); and a **fully restructured `README.md`** (grouped feature list reflecting everything shipped, corrected deps — **`viu` removed**, art is in-project OpenCV half-blocks — accurate playback-controls table, docs section, Settings-based configuration, MP3-vs-MP4 notes). All internal links verified.
- ⬜ **#65 — Keep this TODO current** (add anything discussed/fixed, with correct marks) — ongoing.
- ✅ **#70 — Up-to-date docs everywhere.** All source modules have a module docstring; **per-function docstring coverage swept from 43% → 94%** (549/582) this session — every module-level function and class method is documented; the remaining ~33 are intentionally-skipped trivial redraw/clamp closures (a docstring there is noise). pyright 0/0.
- ✅ **#72 — Dependencies synced.** `pyperclip` bound aligned across both files; `colorama` is now actually used (`just_fix_windows_console()` in `main`); used imports all match the declared deps.

## Cross-platform

- ⬜ **#73 — Mobile/terminal robustness** (NOT URGENT). Investigate the mobile-terminal segfault; improve general cross-compatibility.

---

## IDEA — pattern-based bulk editing

A richer bulk-edit mode driven by patterns. **Mostly shipped** — derive from filename/folders (#83), the `%token%`/regex template overrides, standalone sort orders, ranged/periodic/date-schedule assignment, the disc↔continuous renumberer, **regex in the Set-value op**, **rename files from tags**, and **per-file album art** all landed. **Nothing open** — the last idea, *regex in the Rename-tags op*, was **scrapped** (renaming only changes the 4-letter frame key, so a value find/replace there just duplicates *Set value*). Bulk ops now live under *Edit tags → Operation*, a **two-level menu**: **TAGS** (Add new tag · Set value · Rename tags · Delete tags) and **Automation…** (Derive from filename · Rename files from tags · Set album art from files · Assign by range/schedule · Apply sort orders · Renumber tracks · Copy from first track).

- ✅ **Smart sort orders in bulk** — DONE within #83 for the derive pass, and now a standalone **"Apply sort orders"** bulk op (`apply_sort_orders`): pick which of TSOP/TSO2/TSOA/TSOT to (re)generate from each file's existing artist/album-artist/album/title via the #42 engine (name-inversion for artists, article-move for album/title, nothing when the value sorts as itself, VA/mononyms left alone), fill-blanks/overwrite + preview; MP3/ID3 only. End-to-end write/read verified. Still nice-to-have: a user-verifiable/chosen output *pattern* for multi-artist (`Jeff Goldblum & The Mildred Snitzer Orchestra Feat. dodie` → `Goldblum, Jeff/Mildred Snitzer Orchestra, The/dodie`).
- ✅ **Ranged / periodic assignment** — new bulk op **"Assign by range / schedule"** (`assign_by_pattern` + pure `src/bulk_pattern.py`). Tracks are ordered by (disc, track, filename); then a chosen tag is assigned by: **Ranges** (from/to grid → value, with `{n}` = range index), **Every-N** (group every N → template with `{n}`, e.g. `Series {n}`), or a **Date schedule** for ISO8601 tags (start date + interval, stepping **per-track / per-disc / per-group-of-N**, with an optional **time of day** — a single time for all or a **per-group/per-series time**, e.g. Series 1 at 18:30 & Series 2 at 11:30 → full ISO timestamps). Preview (position · file · value) + fill-blanks/overwrite + confirm; MP3/ID3 only (writes any text/date frame via `create_frame`). Pure assignment logic unit-tested; end-to-end write/read verified (TSST ranges, TDRC per-disc dates + times). *Covers the old "periodic dates" incl. per-series times, and "periodic/ranged text".* Still nice-to-have: non-positional per-metadata rules.
- ✅ **Movement ↔ disc track-numbering converter** — bulk op **"Renumber tracks (disc ↔ continuous)"** (`renumber_tracks_op` + `bp.renumber_tracks`): converts track numbers between **continuous** (album-relative / movement systems — 1…N across all discs, total = N) and **per-disc** (disc-relative — restart at 1 each disc, total = disc count). Ordered by (disc, track); old→new preview; writes via `tag_writer` so **MP3 (TRCK) and MP4 (trkn)** both work. Pure logic + both-format write verified.
- 🟡 **Regex support** — DONE for the derive op (both A & B): a "Use a regex" detection mode takes a raw Python regex with named groups (`track`/`disc`/`title`/`artist`/`albumartist`/`album`/`year`/`date` + `season`→disc, `episode`→track aliases). Matches either the **file name** (A) or the **folder path from the library root** (B) — chosen via a "Match regex against" sub-prompt that shows a live sample; `/`-separated, extension dropped, so one regex can capture `Artist/Album/…` folder levels. Overrides auto-detection (folder-derived fields still fill uncaptured ones); non-match → auto fallback + note; unrecognised groups reported. Documented in `docs/library-layout.md`. ✅ **Also DONE for the "Set value" bulk op:** a "Value source" sub-prompt offers *Enter a value* (literal, as before), *Find & replace (regex)* — `re.sub(pattern, replacement, …)` over each **existing** value of the target frame, applied value-by-value so multi-value frames transform in place — and *From file name / folder (regex)* — matches the file name or library-relative folder path (reusing derive's `_regex_target`/`_derive_regex_base`) and expands a template (`\1` / `\g<name>`) into the value. Per-file computation (`_compute_set_value`); no-match/inapplicable leaves the frame untouched (only files already carrying the tag are touched, mirroring literal Set — use *Add New Tag* to populate empties). Both modes + the end-to-end write verified headlessly. **Rename** op: regex there was **scrapped** — it renames the frame *key* (a 4-letter ID3 code) only, so a value find/replace would just duplicate Set's, and regexing the key itself is pointless.
- ✅ **Rename files from tags** — the inverse of Derive: **Automation… → Rename files from tags** (`rename_files_op` + pure `src/id3/file_namer.py`). Expands a `%token%` pattern from each file's tags into a clean name. **Rich token set** (~32: ), a **preset picker** + **custom pattern** + a **"Show all tokens"** reference screen. **Smart default:** includes `%artist%` (`%track% %artist% - %title%`) only when track artists vary across the selection (compilation), else `%track% %title%`. Numbers zero-padded (to the total's width), filesystem-illegal chars stripped, separators orphaned by an empty token cleaned up. Overflow-safe **preview** (`pos · old → new`) with Space-deselect; **collision-safe two-phase rename** (source→temp→final, so a target equal to another selected file's current name — even a full A↔B swap — never clobbers) and it keeps the **library paths in sync**. MP3 (full token set) + MP4 (common atoms). Pure logic unit-tested; end-to-end rename incl. swap verified headlessly.
- ✅ **Per-file album art** — bulk op **"Set album art from files"** (`set_album_art_op` + pure `src/id3/cover_matcher.py`) embeds a distinct cover per track by pairing each track with the image file that belongs to it (a folder of `01 - Song.jpg` …, or a `covers/1.png` … pool; images are discovered beside the tracks **and** in `artwork/covers/scans/…` subfolders, jpg/png/gif/bmp/webp). **Five pairing modes:** *Auto-detect* (greedy 1:1 — scores every track↔image pair by exact-name → track-number → title-word/substring match, generic whole-album names like `folder.jpg` ranked low so they never out-rank a real per-track cover, then assigns best-first so two tracks never fight over one image; **falls back to one-cover-per-group** via `plan_best` when there are ≥2 real groups each matched to a distinct image and grouping covers more than per-track — so a series whose art is per-season "just works"), *One cover per disc / series / work* (`plan_grouped` — groups tracks by disc tag / `SxxExx` season / work-grouping and shares one cover across the whole group, matching a numbered group to the image bearing that number, e.g. `Cover 1.jpg`→all of season 1; a "Group by" sub-prompt overrides the auto axis), *Matching file name* (strict same-stem), *Track number / order* (positional zip, ordered by disc/season then track), and *Name pattern* (`%token%` rendered into the expected image name). Pairing is **per-directory** so a box set's disc folders keep their own art. **Preview:** track · matched cover · confidence, Space to toggle, and **`d` opens a ranked per-file picker** (best guess first, size + confidence hints, *Type a path…* / *No art for this file* escapes) to choose or change any track's cover — updates the row in place. **Write policy** sets the initial checkboxes (fill-blanks vs overwrite), but the checkboxes are the final word (an explicit pick always writes). **Cross-format writer** (`tag_writer.write_cover`/`has_cover`, new): MP3 **APIC** (fresh ID3 on a blank file, replaces existing APIC) and MP4 **`covr`** atom — the latter is JPEG/PNG only, so other formats are skipped **and said so** (not silent). `WriteResult` gained `skipped_format`. **Single-track too:** the tag editor's Add-Tag / Edit APIC flow now recommends the most likely nearby cover first (same ranked picker) via a threaded `file_path` on `prompt_for_value`, instead of a blind path prompt. Pure matching/ranking + all four plan modes + MP3 & MP4 write round-trips + real-tag end-to-end pairing (generic decoy avoided) verified headlessly. *Wants a live TUI pass on the preview + `d` picker.* MP3/MP4 only.

(Depends partly on the tag/filesystem-etiquette docs, #48 — which should now document exactly the naming conventions #83's parser understands.)

---

## Live TUI pass — ✅ ALL CONFIRMED on a real terminal
*(Everything below was checked live and is working; kept as an archived record of what was verified. Nothing in this section is outstanding.)*

- **#13** — `w` cycle renders in wide + standard; in minimal (<60 cols) the right column isn't drawn, so cycling there only hides lyrics.
- **#21** — volume bar draws in the split view (gutter between art and pane); suppressed only in minimal layouts. Check the gutter spacing.
- **#74** — EQ applied via libvlc; the audible effect needs a real play. If it doesn't take on the first instant, move `set_equalizer` to just after `play()`.
- **#16/#75** — the graphic-EQ widget renders headlessly; check mouse column mapping, curve overlay, presets live.
- **#10** — q-in-editors uses a deferred-quit flag (editor commits → caller saves → next menu unwinds). Confirm the save lands before exit.
- **#35** — lyric-editor mouse: confirm click-to-row lands on the right line (mapped by header height + item rows).
- **#39** — hollow-cursor-on-blur relies on terminal focus reporting (`\033[?1004h`); confirm the cursor goes hollow when the window loses focus (and that supported terminals don't emit stray input).
- **#50** — low volume: confirmed audible on a live test (only 0% silent). Resolved, no change needed.
- **#82** — playback metadata: relaunch the player, press `m`, confirm `year · genre · …` appears (verified headlessly + fixed TYER/frame-buffer bugs this session). If still wrong, add the frame-dump debug.
- **#81** — confirm the transport controls don't shift when pressing `+`/`-`.
- **APIC add (fixed this session):** adding an APIC via "Add Tag" now saves (was a dead `frame_type=='IMAGE'` check; now dispatches on `ui_category=='image'`, and the chosen picture type/description are applied). Worth a quick live confirm that art shows after adding.
- **#8 — live search widget (`prompt.live_select`):** confirm typing responsiveness on a real library, matched-char highlighting, arrow/scroll navigation, `Tab` scope cycling, and the 6-column layout on a narrow terminal. Re-runs the matcher per keystroke (200-result cap) — if it lags on a large library, add debouncing. Ranking/highlight/rows verified headlessly.
- **#83 — derive preview + `d` detail view:** confirm the multi-select preview, the `d` full-detail nested prompt (mouse re-arm + redraw on return), and that writes land (MP3 + MP4). Parser/writer verified headlessly.
- **#84 — bulk people editor + `K`/`J` reorder + `f` import:** confirm the aggregate/edit/add/remove flow across files, reorder in the list editor, and the path-prompt import (fresh screen). Pure logic + round-trips verified headlessly.
- **#85 — arrow-key latency:** confirm single `←`/`→` taps in playback now respond like the letter keys.
- **#86 — help hints persist:** confirm the hint bar only toggles on `i` and doesn't flicker on play/pause/seek/volume.
- **#21 (volume gaps) — confirm the bar is gap-free at non-100 % levels** after the whole-cell-fill change.
- **Bulk pattern ops** — walk the four new operations end-to-end on a real album/box set: *Assign by range/schedule* (ranges grid, every-N, date schedule + times), *Apply sort orders*, *Renumber tracks* (disc↔continuous), and confirm the previews/writes land. All pure logic + end-to-end writes verified headlessly; the prompts/list_edit grid/date pickers want eyes.
- **#62 — Ctrl-T value toggle + auto-play:** confirm `Ctrl-T` flips a value edit between the smart widget and raw text (dates/people especially), and that *Toggle Auto-play on Select* skips the action menu without looping on multi-action tracks.
- **#44 — history columns:** confirm the aligned columns + relative timestamps render cleanly.
- **#48 — plain-text editing:** confirm the Settings toggle routes date/fraction/people edits to raw text (system editor for people).
- **#60 — multi-value tag editor:** on a real terminal, edit a multi-value frame (genre/artist/composer/conductor/mood) — confirm it opens as a **plain text field** for a 0–1-value frame, `Ctrl-T` expands to the single-column `list_edit` (add/edit/delete/reorder `K`/`J`), a frame with 2+ values **auto-opens the list**, a half-typed value carries across the `Ctrl-T` toggle, and the saved file reads back all values (v2.4 for multi, v2.3 for single). Field/list dispatch, cardinality default, and toggle-carry verified headlessly.
- **Rename files from tags:** on a real album, walk *Edit tags → Automation… → Rename files from tags* — confirm the suggested pattern (artist folded in only for compilations), the "Show all tokens" reference, a custom pattern, the `old → new` preview/deselect, and that files rename on disk (incl. a compilation) with the library staying in sync. Pure logic + two-phase rename (incl. swap) verified headlessly.
- **#4 — even margins:** on a real terminal with zero margin, confirm the bulk/single-track rounded box headers now have equal left/right padding, the `text()`/`path()` input frames sit even, rules still span full width, and album art still fills narrow windows. Margin math verified headlessly.
- **#16 rating / numeric / enum widgets:** on a real track, edit **POPM** (Add Tag `POPM`) — confirm the star editor sets 0–5★, Tab reaches Plays + Rater, saves, and the list shows `★★★★☆ (N plays)`. Edit **PCNT**/**RBUF** and an `INT_BIG` tag (**TBPM/TLEN/TDLY**) — the `number_edit` spinner (↑↓/PgUp-Dn/type) and `Ctrl-T` raw toggle on the INT_BIG ones. Edit **TKEY** (key picker + custom), **TMED** (media picker + custom), **TSRC** (ISRC validation warning on a bad code), **TCMP** (Yes/No), and confirm **RVRB** shows the "not editable" message. All create/save/read/summarize round-trips + ISRC regex verified headlessly (18/18).
- **Per-file album art:** walk *Edit tags → Automation… → Set album art from files* on a real album/box set — confirm each match mode (auto/name/track-order/`%token%`), the preview's `d` per-file picker (ranked, *Type a path…*, *No art for this file*), fill-blanks vs overwrite defaults + checkbox override, and that art embeds (MP3 APIC + MP4 `covr`; non-JPEG/PNG MP4 skipped-with-note) and shows on the next play. Also the **single-track** Add-Tag/Edit APIC picker recommending the nearest cover first. Pure logic + both write formats + real-tag pairing verified headlessly.
- **Regex in Set value:** walk *Edit tags → Set value → Value source*: (1) *Find & replace (regex)* transforms existing values (incl. value-by-value on a multi-value frame); (2) *From file name / folder (regex)* matches the name/library-relative path and expands `\1`/`\g<name>` into the value. Confirm the sample-string prompt, no-match leaves frames untouched, and only tag-bearing files are changed. `_compute_set_value` + writes verified headlessly.
