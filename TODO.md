1.  implement addition nav options with b/→/← etc.
2.  make lead-in time ui cleaner
3.  tidy up clear history log
4.  add margins globally (except album art in standard and narrow views, and horizontal rules)
5.  when a setting is changed, the ui should stay on the current item rather than going to the top
6.  add clear library xml path if not wanted
7.  section settings
8.  improve search results to make more obvious the findings
9.  in artist and genre browse, it should show an album list within each artist even if only one album (albums are different)
10. quit should be global exit to terminal
11. back should be universal back button
12. if only one track in an album or group, don't display play all button or bulk edit button
13. add queue list view to playback
14. allow playback pop out into another window, so you can still browse/edit
15. ensure bulk editor uses specific prompts
16. audit all tags in registry to ensure the are using the proper input method
17. ensure bulk edit boxes at the top span the whole width
18. POST-DEV: ensure default options are for a more general userbase, not for me testing
19. generalise language for audio rather than just radio content
20. ensure album and artist always displays, then metadata toggle is only for year, disc, movement, track, etc.
21. visual volume indicator
22. ensure all hints are logical
23. consider a more tui shortcut system similar to vim etc.
24. lock mouse clicks to only activate when over text or button, not empty space in the same row
25. make track items have more data if e.g. different track to album artists, duration etc.
26. filenames may be esoteric so use track data instead for titles etc. (have obviously seperate filepath tag field in metadata that is made clear is not id3)
27. remove add tag button, make a shortcut instead